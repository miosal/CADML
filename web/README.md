# CADML web

The CADML homepage. A Next.js + Tailwind app that introduces the
language and demos the C++ reference compiler running in your browser
via WebAssembly.

Lives under `cadml-oss/web/`. The parent repo is the CADML language
spec and reference toolchain.